<h1>Mis posts</h1>
<table class="table">
	<tr>
		<th>Titulo</th>
		<th>Texto</th>
		<th>N° comentarios</th>
		<th></th>
		<th></th>
		<th></th>
	</tr>	

	<% @posts.each do |post| %>
	<tr>
		<td><%= post.title %></td>
		<td><%= post.text %></td>
		<td><%= post.comments.count %></td>
		<td><%= link_to 'Show', post_path(post), :class => 'btn btn-success btn-sm' %></td>
		<td><%= link_to 'Edit', edit_post_path(post), :class => 'btn btn-warning btn-sm' %></td>
		<td><%= link_to 'Destroy', post_path(post), method: :delete, data: {confirm: 'Estas segurin???'}, :class =>'btn btn-mini btn-danger btn-sm' %></td>
	</tr>
	<% end %>
</table>

<%= link_to 'Nuevo Post', new_post_path %>